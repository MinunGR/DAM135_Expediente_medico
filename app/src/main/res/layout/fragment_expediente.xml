<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="0dp">

    <!-- Título -->

    <ImageView
        android:id="@+id/imageView15"
        android:layout_width="205dp"
        android:layout_height="231dp"
        android:layout_marginTop="236dp"
        android:layout_marginEnd="92dp"
        android:elevation="-15dp"
        android:rotation="-70"
        android:rotationX="-147"
        android:rotationY="-2"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/bg_circle_1" />

    <ImageView
        android:id="@+id/imageView14"
        android:layout_width="327dp"
        android:layout_height="353dp"
        android:layout_marginStart="28dp"
        android:layout_marginTop="-200dp"
        android:elevation="-15dp"
        app:layout_constraintStart_toEndOf="@+id/imageView13"
        app:layout_constraintTop_toBottomOf="@+id/listHistorial"
        app:srcCompat="@drawable/bg_circle_1" />

    <TextView
        android:id="@+id/lblListHistorico"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="75dp"
        android:gravity="center"
        android:text="@string/historico_citas"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Etiqueta de Paciente -->

    <!-- Spinner de pacientes -->
    <Spinner
        android:id="@+id/slcPacientes"
        android:layout_width="360dp"
        android:layout_height="50dp"
        android:layout_marginTop="17dp"
        android:background="@color/colorAccent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/lblListHistorico" />

    <!-- Lista de historial -->

    <!-- Botón flotante -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/listHistorial"
        android:layout_width="360dp"
        android:layout_height="450dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="30dp"
        app:layout_constraintBottom_toTopOf="@+id/btnAgregarHistorico"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.516"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/slcPacientes" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnAgregarHistorico"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="169dp"
        android:layout_marginTop="25dp"
        android:layout_marginEnd="169dp"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="@color/colorAccent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.515"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/listHistorial" />

    <ImageView
        android:id="@+id/imageView13"
        android:layout_width="327dp"
        android:layout_height="353dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="552dp"
        android:elevation="-15dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnAgregarHistorico"
        app:srcCompat="@drawable/bg_circle_1" />

</androidx.constraintlayout.widget.ConstraintLayout>
